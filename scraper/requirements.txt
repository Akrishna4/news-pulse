# News Pulse — Python pipeline dependencies
# Installed during Phase 2/3 work and baked into the Docker image in Phase 8.
# Pinned to major versions now; exact patch versions locked once installed.

feedparser>=6.0,<7.0
trafilatura>=1.9,<2.0
lxml_html_clean>=0.1,<1.0    # required by trafilatura when lxml>=5.x splits html.clean
httpx>=0.27,<1.0
psycopg2-binary>=2.9,<3.0
python-dateutil>=2.9,<3.0
certifi>=2024.0,<2026.0
python-dotenv>=1.0,<2.0
