# News Pulse — frontend environment template
#
# NOTE: NEXT_PUBLIC_* vars are inlined into the JS bundle at BUILD time, not
# read at runtime. If the backend URL changes after first deploy, this must
# be updated on Vercel and the frontend redeployed — updating the env var
# alone is not enough.

NEXT_PUBLIC_API_URL=http://localhost:4000
